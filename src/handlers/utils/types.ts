export enum BridgeType {
    BridgeIn = 1,
    BridgeOut = 2
}

export enum ProposalStatus {
    Initiated = "Initiated",
    Voting = "Voting",
    Approved = "Approved",
    Rejected = "Rejected",
    Expired = "Expired",
}