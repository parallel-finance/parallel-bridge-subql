export enum BridgeType {
    BridgeIn = "BridgeIn",
    BridgeOut = "BridgeOut",
}

export enum ProposalStatus {
    Initiated = "Initialized",
    Voting = "Voting",
    Approved = "Approved",
    Rejected = "Rejected",
    Expired = "Expired",
}